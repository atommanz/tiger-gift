---
name: 02-start-e2e-test
description: Run end-to-end testing of the application. Start the app if not running and verify all critical paths work correctly.
disable-model-invocation: true
---

Start the app if not running and test and verify the critical path end-to-end. Make sure that every function works before delivering to users. Show test results in table format.

**IMPORTANT**: After completing each test, immediately mark it as done by changing `[ ]` to `[x]` in this file. Do this after each individual task completes, not all at once at the end.

Critical paths to test:
- [ ] 1. Dashboard loads and shows summary data
- [ ] 2. Main user interaction flow works correctly
- [ ] 3. AI-powered features return valid results
- [ ] 4. UI displays results in the expected format
